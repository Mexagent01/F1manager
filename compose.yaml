services:
  mysql:
    image: mysql:8.0
    container_name: f1manager-mysql

    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: school

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: